[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    # Allow camera access for computer vision
    Permissions-Policy = "camera=(self)"
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
